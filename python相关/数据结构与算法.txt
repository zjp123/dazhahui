timert 模块可以测试代码执行的时间或者  start = time.time()   end = time.time()  end - start

操作系统标识的内存最小的单元为字节  

python中， a = 10 是两块内存，10是一块内存，a是一块内存，a内存保存的是10的内存的地址

self.__head 这是私有属性

链表的优点是可以利用内存当中的分散内存，缺点是内存增加了，顺序表要求内存必须是连续的

冒泡排序：
  冒泡排序每次比较两个元素，把较大的数和小的数进行交换，那么每次循环结束都会把当前最大的数，找出来。

